using System;
using System.IO;
//clean code competitive
class TWK {
    static void buffIO(){Console.SetIn(new StreamReader(Console.OpenStandardInput()));Console.SetOut(new StreamWriter(Console.OpenStandardOutput()));}
    static void file(string x){if(x=="")return;Console.SetIn(new StreamReader(x+".inp"));Console.SetOut(new StreamWriter(x+".out"));}
    
    
    
    static void Main() {
        buffIO();
        file("");
        //code here
        
        
        
        //end code
        Console.Out.Flush();
    }
}
